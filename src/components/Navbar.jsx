import React, { useState } from "react";

export const Navbar = () => {
  // const [mode, setMode] = useState("Dark");

  return (
    <div className="nav">
      <h1>Where in the world ?</h1>
    </div>
  );
};
